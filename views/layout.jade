doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(href="//maxcdn.bootstrapcdn.com/bootswatch/3.3.0/flatly/bootstrap.min.css", rel="stylesheet")
    link(href='//getbootstrap.com/examples/dashboard/dashboard.css' rel='stylesheet')
    script(src='//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js' )
    script(src='//maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js' )
    script= 'url = "localhost:3000"; console.log("connect"); $("#getTests").on("click",getTestsList()); $("#getUsers").on("click",getUsers()); function getUsers(){ $.ajax({ url: url+"/account/getUsers", context: document.body }).done(function() { }); } function getTestsList(){ $.ajax({url: url+"/account/getTestlist", context: document.body }).done(function() {});}'
  body
    block content
